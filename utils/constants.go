package utils

// Tree node and context constants
const (
	// TreeNodeVar is the root variable node name in the Tree structure
	TreeNodeVar = "var"

	// ContextKeyAttributes is the key for storing the Tree in the context map
	ContextKeyAttributes = "attributes"

	// ContextKeyFunctions is the key for storing functions in the context map
	ContextKeyFunctions = "functions"
)
